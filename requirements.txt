# ============================================================
# 核心依赖（SQLite 模式必须）
# ============================================================
websockets>=12.0
apscheduler>=3.10.0
flask>=2.3.0
flask-cors>=4.0.0
pyyaml>=6.0
bcrypt>=4.0.0
requests>=2.28.0
psutil>=5.9.0
waitress>=3.0.0              # 生产级 Web 服务器（Web UI 默认使用）

# ============================================================
# MySQL 可选依赖（使用 MySQL 数据库时自动安装，无需手动安装）
# ============================================================
# pymysql>=1.0.0
# DBUtils>=3.0.0

# ============================================================
# 可选依赖（仅当插件需要时安装，框架本体不依赖）
# ============================================================
Pillow>=10.0.0             # 图像处理（图片渲染类插件需要）
numpy>=1.24.0              # 数值计算（部分数据类插件需要）
httpx>=0.27.0              # 异步 HTTP（llm_plugin_gen 等异步插件需要）
